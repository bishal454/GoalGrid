# Live Score Loading Animation

Put the downloaded waiting animation video here:

`frontend/public/animations/live-fetch-loader.mp4`

The dashboard references it in the browser as:

`/animations/live-fetch-loader.mp4`

Use a short, looping MP4 for best browser support. If the file is missing or fails to load, the UI falls back to a CSS spinner.
